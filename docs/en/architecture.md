# Architecture

## Content layers

- `docs/cs/` and `docs/en/` for language-specific content
- `docs/blog/` for articles and release notes
- `docs/api/` for OpenAPI spec
- `docs/versions/` for older releases
- `docs/assets/` for JS, CSS and images
