openapi: 3.1.0
info:
  title: VCCSD Docs API
  version: 2.0.0
  description: Sample OpenAPI specification for the docs release repo.
servers:
  - url: https://api.example.com
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
  /posts:
    get:
      summary: List blog posts
      responses:
        "200":
          description: OK
