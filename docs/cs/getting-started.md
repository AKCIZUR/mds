# Rychlý start

## Instalace

```bash
pip install -r requirements.txt
python scripts/generate_indexes.py
mkdocs serve
```

## Build

```bash
mkdocs build --strict
```

## Doporučení

- uprav `site_url`
- uprav `repo_url`
- doplň vlastní obsah
- spusť GitHub Actions deploy
