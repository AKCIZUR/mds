# Rychlý start

## Instalace

```bash
pip install -r requirements.txt
python scripts/generate_indexes.py
mkdocs serve
```

## Co upravit

- `site_url`
- `repo_url`
- `USERNAME`
- `REPO`
- logo a favicon

## Doporučený build

```bash
python scripts/generate_indexes.py
mkdocs build --strict
```
