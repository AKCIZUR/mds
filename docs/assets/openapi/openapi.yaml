openapi: 3.1.0
info:
  title: Coherent MkDocs API
  version: 2.0.0
servers:
  - url: https://api.example.com
paths:
  /health:
    get:
      summary: Health check
      responses:
        "200":
          description: OK
  /posts:
    get:
      summary: List blog posts
      responses:
        "200":
          description: OK
